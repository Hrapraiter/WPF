<Window x:Class="Binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Binding"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300" ResizeMode="CanMinimize" FontSize="16">
    <Grid Background="#FF1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox        Grid.Row="0" Name="txtInput"
                        Width="250" Height="32"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Text="{Binding BoundText,UpdateSourceTrigger=PropertyChanged , Mode=OneWayToSource}" 
                        Background="#FF2E2E2E" 
                        Foreground="#FFF0EFF1"
                        />
        <Button         Grid.Row="1" Name="btnDefault" 
                        Content="Default"
                        Width="250" Height="32" 
                        Click="btnDefault_Click" Background="#FF9184EE"
                        />
        <TextBlock      Grid.Row="2" Name="tbResult" 
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Text="{Binding BoundText}"
                        Foreground="#FFF0EFF1"
                        />
    </Grid>
</Window>
